-- ============================================================
--  0020 — รูปโปรไฟล์ผู้ใช้ (avatar)
--  เก็บเป็น data URL (รูปย่อ) หรือ URL ภายนอกก็ได้
-- ============================================================
alter table public.profiles add column if not exists avatar_url text;
